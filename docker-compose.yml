services:
  xray-kuma-probe:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./generated:/app/generated
    ports:
      - "127.0.0.1:8080:8080"
    stop_grace_period: 20s
    security_opt:
      - no-new-privileges:true
